@model List<Counts>
@{
    int counter = 0;
}

<div class="container">
    <div class="d-flex" style="justify-content: space-around; align-items:center">
        <h1>Counts</h1>
        <a class="btn btn-success" asp-action="Create" asp-controller="counts">Create</a>
    </div>


    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th scope="col">№</th>
                <th scope="col">Id</th>
                <th scope="col">Icon</th>
                <th scope="col">Number</th>
                <th scope="col">Title</th>
                <th scope="col">Description</th>
                <th scope="col">RedirectUrl</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                counter++;
                <tr>

                    <th scope="row">@counter</th>
                    <td>@item.Id</td>
                    <td>@item.Icon</td>
                    <td>@item.Number</td>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@item.RedirectUrl</td>
                    <td>
                        <a class="btn btn-primary" asp-action="edit" asp-controller="counts" asp-route-id="@item.Id">Edit</a>
                    </td>
                    <td>

                        <a class="btn btn-danger" asp-action="delete" asp-controller="counts" asp-route-id="@item.Id">Delete</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>