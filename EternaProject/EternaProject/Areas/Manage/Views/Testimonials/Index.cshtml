@model List<Testimonials>
@{
    int counter = 0;
}

<div class="container">
    <div class="d-flex" style="justify-content: space-around; align-items:center">
        <h1>Counts</h1>
        <a class="btn btn-success" asp-action="Create" asp-controller="testimonials">Create</a>
    </div>


    <table class="table table-striped table-dark">
        <thead>
            <tr>
                <th scope="col">№</th>
                <th scope="col">Id</th>
                <th scope="col">ImageUrl</th>
                <th scope="col">Fullname</th>
                <th scope="col">Position</th>
                <th scope="col">Description</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                counter++;
                <tr>

                    <th scope="row">@counter</th>
                    <td>@item.Id</td>
                    <td>@item.ImageUrl</td>
                    <td>@item.Fullname</td>
                    <td>@item.Position</td>
                    <td>@item.Description</td>
                    <td>
                        <a class="btn btn-primary" asp-action="edit" asp-controller="testimonials" asp-route-id="@item.Id">Edit</a>
                    </td>
                    <td>

                        <a class="btn btn-danger" asp-action="delete" asp-controller="testimonials" asp-route-id="@item.Id">Delete</a>
                    </td>

                </tr>
            }
        </tbody>
    </table>
</div>